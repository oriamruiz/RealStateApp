@model List<RealStateApp.Core.Application.ViewModels.AdminHome.AdminHomeSaleTypesViewModel>
@{
    ViewData["Title"] = "Mant. de tipo de venta";

}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-9">
            <h2 class="float-start">Mantenimiento de tipo de venta</h2>

        </div>
        <div class="col-3 ">

            <a asp-controller="SaleType" asp-action="Create" class="btn btn-outline-success float-end">Agregar nuevo tipo de venta</a>

        </div>


    </div>

    <div class="row">

        @if (Model == null || Model.Count() == 0)
        {
            <div class="alert alert-warning ms-2" role="alert">
                No hay tipos de ventas creados
            </div>

        }
        else
        {
            @foreach (var saletype in Model)
            {

                <div class="col-3 mb-4">
                    <div class="card shadow-sm">

                        <div class="card bg-light text-center ">
                            <div class="card-body">
                                <p class="card-text custom-paragraph"><strong>Nombre:</strong> @saletype.Name</p>
                                <p class="card-text custom-paragraph"><strong>Descripcion:</strong> @saletype.Description</p>
                                <p class="card-text custom-paragraph"><strong>Propiedades de este tipo:</strong> @saletype.Propertiesquantity</p>
                            </div>

                            <div class="card-body">
                                <div class="btn-group">
                                    <a asp-controller="SaleType" asp-action="Update" asp-route-id="@saletype.Id" class="btn btn-sm btn-outline-info">Editar</a>
                                    <a asp-controller="SaleType" asp-action="Delete" asp-route-id="@saletype.Id" class="btn btn-sm btn-outline-danger">Eliminar</a>

                                </div>
                            </div>
                        </div>


                    </div>
                </div>

            }

        }

    </div>

</div>
